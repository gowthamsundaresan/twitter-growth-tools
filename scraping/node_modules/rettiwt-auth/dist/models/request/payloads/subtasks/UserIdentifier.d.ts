import { ISettingResponse, IUserIdentifierInput } from '../../../../types/request/payloads/subtasks/UserIdentifier';
/**
 * The input data to be sent for identifying the user using email, phone, etc.
 *
 * @internal
 */
export declare class UserIdentifierInput implements IUserIdentifierInput {
    setting_responses: ISettingResponse[];
    link: string;
    /**
     * @param userId The id to be used for identifying the user.
     */
    constructor(userId: string);
}
